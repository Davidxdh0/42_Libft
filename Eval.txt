you still need to check for malloc fails and check for multiple operations in one line. 

Too bad about the norm error, and a few mistakes. But you're doing well, and you'll get there!

Todo: 
Itoa anders maken zonder ft_strjoin
- ft_itoa doesnt allocate enough memory in str
    -   memleak if ft_strjoin returns NULL
ft_split - free 2d arraz when malloc fails
-   ft_strchr - use size_t and remove ft_strlen
substring start na de string checken -- substr("12345", 16, 10000);
segvault bij join

fsanitizeadress als eerst
system("leaks a.out");
Valgrind als laatste check


#include "libft.h"
#include <stdlib.h>

int main(void) {
	char* x = ft_itoa(-2147483648);

	printf("%s\n", x);
	free(x);
	system("leaks a.out");
	return 0;
}